/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env
.env.*
!.env.example
next-env.d.ts
/.claude/settings.local.json
.gstack/

# TypeScript incremental build cache
*.tsbuildinfo

# Schemas staged for the reverse proxy by `npm run schemas`. Derived from the
# protocol repo, which stays their only definition — committing a copy here
# would be a second one, free to drift.
/dist-schemas/
